# Exclude everything by default to keep build context minimal.
*

# Allow only files/directories required by docker/Dockerfile
!uv.lock
!pyproject.toml
!main.py

!app
!app/**

!certs
!certs/localhost.crt
!certs/localhost.key

!docker
!docker/Dockerfile
